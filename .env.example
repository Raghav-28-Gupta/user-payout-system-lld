# PostgreSQL connection string (any Postgres works; Neon/Supabase/local/Docker).
# For Neon: use the DIRECT (non-pooled) endpoint so `prisma migrate` works.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# Port for the HTTP server (optional, defaults to 3000)
PORT=3000
